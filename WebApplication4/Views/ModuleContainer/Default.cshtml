@model IEnumerable<WebApplication4.Models.ModuleContainerViewModel>

@foreach (var moduleDefine in this.Model)
{
    string ctlName = moduleDefine.ControllerName;
    string actName = moduleDefine.ActionName;

    @Html.Action(actName, ctlName, new { ModuleInstanceID = moduleDefine.PageModuleID })
}